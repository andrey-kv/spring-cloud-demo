spring:
  application:
    name: roomreservationservices
server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include:
          - info
          - beans
          - health
          - metrics
          - env